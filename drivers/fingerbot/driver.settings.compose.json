[
    {
      "id": "fingerbot_mode",
      "type": "dropdown",
      "label": {
        "en": "Operating Mode (if supported)"
      },
      "value": "click",
      "values": [
        {
          "id": "click",
          "label": {
            "en": "Click (push and release)"
          }
        },
        {
          "id": "switch",
          "label": {
            "en": "Switch (toggle position)"
          }
        },
        {
          "id": "program",
          "label": {
            "en": "Program (custom sequence)"
          }
        }
      ]
    },
    {
      "id": "lower_limit",
      "type": "number",
      "label": {
        "en": "Down Movement Limit (%) (if supported)"
      },
      "value": 100,
      "min": 50,
      "max": 100,
      "units": "%",
      "hint": {
        "en": "How far down the finger moves (50-100%) (Behavior depends on device firmware)"
      }
    },
    {
      "id": "upper_limit",
      "type": "number",
      "label": {
        "en": "Up Movement Limit (%) (if supported)"
      },
      "value": 0,
      "min": 0,
      "max": 50,
      "units": "%",
      "hint": {
        "en": "How far up the finger moves (0-50%) (Behavior depends on device firmware)"
      }
    },
    {
      "id": "sustain_time",
      "type": "number",
      "label": {
        "en": "Sustain Time (seconds) (if supported)"
      },
      "value": 0,
      "min": 0,
      "max": 10,
      "units": "s",
      "hint": {
        "en": "How long to hold the button (0-10s) (Behavior depends on device firmware)"
      }
    },
    {
      "id": "reverse_direction",
      "type": "checkbox",
      "label": {
        "en": "Reverse Direction (if supported)"
      },
      "value": false,
      "hint": {
        "en": "Reverse the push direction (Behavior depends on device firmware)"
      }
    }
  ]